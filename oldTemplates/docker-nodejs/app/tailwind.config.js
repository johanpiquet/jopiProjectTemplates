/** @type {import('tailwindcss').Config} */

// This file enables tailwind support for WebStorm.
// It has no other meaning and is not used for tailwind config.

module.exports = {
    content: [
        "./src/**/*.tsx",
    ],
    theme: {
        extend: {},
    },
    plugins: [],
}